<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.apppro.pikturesque">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Getting_Started.SignUpOrLoginActivity" />
        <activity android:name=".User.UserSettingsActivity" />
        <activity android:name=".User.UserActivity" />
        <activity android:name=".Notifications.NotificationsActivity" />
        <activity android:name=".Camera.CameraActivity" />
        <activity android:name=".Search.SearchActivity" />
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".Getting_Started.LoginActivity" />
        <activity android:name=".Getting_Started.SignUpActivity" />
        <activity android:name=".Getting_Started.LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>